# Sample Space

Synthetic pipeline output so you can see `cues.json` without downloading a replay.

This is **not** a real Space. The player on the git page uses these cues. Swap them for your own `cues.json` after `space-tape transcribe`.

```
start, end, speaker, text
```

Keep the original X URL next to any real transcript.
